services:
  openclaw-node:
    build:
      context: .
      args:
        OPENCLAW_BASE_IMAGE: ${OPENCLAW_BASE_IMAGE:-ghcr.io/openclaw/openclaw}
        OPENCLAW_BASE_IMAGE_TAG: ${OPENCLAW_BASE_IMAGE_TAG:-2026.5.2}
    image: ${OPENCLAW_NODE_IMAGE_REPO:-openclaw-node-bastion}:${OPENCLAW_BASE_IMAGE_TAG:-2026.5.2}
    container_name: openclaw-node-bastion
    restart: unless-stopped
    privileged: true
    network_mode: host
    user: "0:0"
    environment:
      HOME: /home/openclaw
      OPENCLAW_STATE_DIR: /home/openclaw/.openclaw
      OPENCLAW_NODE_GATEWAY_HOST: ${OPENCLAW_NODE_GATEWAY_HOST:-openclaw.tail651fca.ts.net}
      OPENCLAW_NODE_GATEWAY_PORT: ${OPENCLAW_NODE_GATEWAY_PORT:-443}
      OPENCLAW_NODE_DISPLAY_NAME: ${OPENCLAW_NODE_DISPLAY_NAME:-bastion}
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN:?set OPENCLAW_GATEWAY_TOKEN in .env}
    volumes:
      - /:/host:rw
      - /home/opc:/home/opc:rw
      - openclaw-node-state:/home/openclaw/.openclaw
    healthcheck:
      test:
        - CMD-SHELL
        - ps -ef | grep -q '[o]penclaw-node'
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    command:
      - openclaw
      - node
      - run
      - --host
      - ${OPENCLAW_NODE_GATEWAY_HOST:-openclaw.tail651fca.ts.net}
      - --port
      - ${OPENCLAW_NODE_GATEWAY_PORT:-443}
      - --tls
      - --display-name
      - ${OPENCLAW_NODE_DISPLAY_NAME:-bastion}

volumes:
  openclaw-node-state:
